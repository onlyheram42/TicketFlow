<UserControl x:Class="TicketFlow.Desktop.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border Background="White" CornerRadius="8" Padding="30" Width="360" MaxHeight="350"
                VerticalAlignment="Center" HorizontalAlignment="Center">
            
            <Border.Effect>
                <DropShadowEffect Color="#d1d5da" BlurRadius="10" Direction="270" ShadowDepth="4" Opacity="0.5"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="Welcome back" FontSize="24" FontWeight="SemiBold" 
                           Foreground="#24292e" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <TextBlock Text="Username" Foreground="#586069" Margin="0,0,0,4"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                         Padding="8" Margin="0,0,0,15" FontSize="14" BorderBrush="#e1e4e8" BorderThickness="1"/>

                <TextBlock Text="Password" Foreground="#586069" Margin="0,0,0,4"/>
                <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" 
                         Padding="8" Margin="0,0,0,20" FontSize="14" BorderBrush="#e1e4e8" BorderThickness="1">
                    <!-- Note: For a real app, use PasswordBox + attached property or code-behind. 
                         TextBox is used here for simplicity in MVVM data binding for the assignment. -->
                </TextBox>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="#cb2431" TextWrapping="Wrap" 
                           Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"
                           Margin="0,0,0,10"/>

                <Button Content="Sign in" Command="{Binding LoginCommand}" 
                        Background="#2ea44f" Foreground="White" FontWeight="Bold" 
                        Padding="10" FontSize="14" BorderThickness="0" Cursor="Hand"
                        IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}"/>

                <ProgressBar IsIndeterminate="True" Height="4" Margin="0,10,0,0" 
                             Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
